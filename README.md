# 绝顶赵个人博客

> 这份 README 是给**未来的 AI 助手**看的项目操作手册——新会话开始时先读它，再动手改代码。

## 项目是什么

绝顶赵（`JUEDING ZHAO`）的个人博客：一个展示「适应性平衡理论」核心思想、执行力系列文章、AI 工具与付费服务的静态网站。

- **仓库**：`https://github.com/992939504/syxph.git`（master 分支）
- **线上地址**：`https://syxph.363749768.xyz/`（Cloudflare Pages，自动部署，push 后约 1-2 分钟生效）
- **版本**：v4 设计系统（黑白撞色 × 暖黄），前身 v3（纸底靛蓝）

## 核心架构（改代码前必读）

- **纯静态、全内联、单文件架构**：9 个 HTML 文件互相独立，无框架、无构建、无服务器依赖。每个页面 CSS/JS 全部内联在单个 HTML 里，浏览器直接双击打开就能完整渲染。
- **SITE 常量机制**：每个页面 head 里有 `const SITE = { name: "绝顶赵", latin: "JUEDING ZHAO" }`，配合 `data-site-name` / `data-site-latin` 属性占位。改名字只改 SITE，全页自动跟随。新页面必须保留这个机制。
- **唯一外部依赖**：Google Fonts（Space Grotesk 500/700，仅首页 + 文章页 + 模板使用）。离线时自动回退系统字体，页面不破。工具页刻意零外部请求。
- **图标**：全部用 Lucide 内联 SVG（24×24、stroke 1.75）。**全程零 emoji**——任何新内容禁止出现 emoji 字符。
- **favicon**：data URI 内嵌（黑色圆角方块 + 黄色「赵」字），零请求。

## 文件清单

| 文件 | 说明 |
|---|---|
| `index.html` | 首页（旗舰沉浸式：预载动画 / 自定义光标 / 视差 hero / 文章列表 / 工具宫格 / 服务区 / 关于区 / 联系区 / 页脚） |
| `专栏/适应性平衡理论.html` | 核心理论文章（V3.2 全文） |
| `专栏/获得执行力（上）.html` | 执行力系列 · 上 |
| `专栏/获得执行力（中）：为什么我们无法行动？.html` | 执行力系列 · 中 |
| `专栏/获得执行力（下）：行动的方法.html` | 执行力系列 · 下 |
| `show.html` / `drive-test.html` / `test.html` | 三个工具页（拆书 / 执行力测试 / 测试） |
| `golden-task.html` | 工具页：黄金任务法（多维度评分 × 权重排序的任务优先级计算器，单文件零外部请求，微信二维码 base64 内嵌；前身是已注销域名的 Golden-Task-Calculator 仓库，2026-08 移植重制） |
| `文章模板.html` | 新文章的标准模板（含全部组件用法注释） |
| `admin/` | 文章管理后台（可视化编辑器 + 发布服务，见下方专节） |
| `启动后台.sh` / `启动后台.bat` | 管理后台快速启动脚本（macOS/Linux / Windows） |
| `适应性平衡理论-完整版-V3.2.md` | 理论文章源文档（被 .gitignore 忽略，不入库） |

## 文章管理后台（写新文章的第一选择）

`admin/` 是一个零依赖的本地管理服务（Node 18+，不需要 npm install），提供**左预览 / 右 Markdown 的分屏编辑器**，并自动完成「如何添加新文章」里的全部手工步骤。

- **启动**：双击 `启动后台.bat`（Windows）或运行 `./启动后台.sh`（macOS/Linux），浏览器会自动打开 `http://127.0.0.1:8618`（高位端口，PORT 环境变量可改）。
- **写文章**：左边是成品效果（与线上同一套模板 CSS），右边写 Markdown 原文，实时预览。标题 / 日期 / 黄色标签 / 系列 / 副标题 / 首页摘要 / 上一篇下一篇都在顶部表单里填。
- **Markdown 语法**：标准语法之外有站点专属写法（编辑器右上角「帮助」有完整说明）：
  - `## 章节` → section-heading，`### 分节` → sub-heading
  - `**加粗**`、`==高亮==`（黄底黑字）、`++关键词++`（黄色马克笔）、`` `代码` ``
  - `> 一句话金句` → quote-block
  - `::: qa` / `::: case` / `::: framework` / `::: highlight` / `::: formula` / `::: note` → 对应站点卡片（工具栏一键插入）
  - `| 表格 |`、`- 列表`、`1. 编号`、`---` 分隔线、` ```标题 ` 代码块（formula-block 样式）
  - 正文可直接写 HTML（如带 Lucide 图标的条款列表会原样透传）
- **保存 / 发布**：Ctrl+S 存草稿（`admin/data/drafts/*.md`，不入库）；「发布」自动做四件事：① 由 `文章模板.html` 生成 `专栏/xxx.html`（文件名自动去掉全角冒号/问号，遵守 CDN 踩坑约定）② 首页文章列表插入新行并重排编号 ③ 归档筛选器月份与篇数更新 ④ 改动前自动备份 index.html 到 `admin/data/backups/`。「下线」只移除首页条目、文件保留。
- **上线**：「Git 推送上线」按钮 = `git add -A → commit → push`，Cloudflare 自动部署；提交信息自动带文章标题。也可以照旧手动 git 操作。
- **注意**：后台只监听本机 127.0.0.1；草稿与备份在 .gitignore 中，不入库。无源稿的旧文章可直接在后台侧栏「站内文章」里下线。

## 如何添加新文章（标准流程）

1. **复制 `文章模板.html`**，重命名放入 `专栏/` 目录。
   - ⚠️ **文件名禁止使用全角冒号「：」和全角问号「？」**——CDN clean URL 和部分内置浏览器会出问题（踩过坑）。用全角括号、句点等安全字符，如 `专栏/我的新文章.html`。
2. **改 `<title>`** 和 SITE 常量保持不动。
3. **文章头部**：标题（空心描边大字，`<span class="accent">` 包几个字可变实心黄）、副标题、`badge-community`（黄色标签，文字可自定义如「社群专享」「核心理论」）、系列标识、日期。
4. **正文**：用模板里的组件，全部在模板正文区有示例——
   - `<p>` 段落、`<h2 class="section-heading">` 章节标题、`<h3 class="sub-heading">` 分节
   - `keyword`（黄色马克笔下划线）/ `highlight`（黄底黑字）
   - `quote-block` 金句卡（黑底黄边）、`highlight-block` 重点卡（黑卡黄边）
   - `qa-block` 问答 / `case-block` 案例 / `framework-block` 框架
   - `ul` 无序列表（黄方块）/ `ol` 编号列表（黄角标 01/02/03）
   - `article-table-wrap` 表格（黄表头硬边，移动端横向滚动）
   - `formula-block` 公式块（黑底黄边，`>` 要写 `&gt;`）
   - `divider` 分隔线、`footer-note` 预告、`promo-block` 锦囊妙计推广
   - 允许/禁止类条款用 Lucide 图标列表，**禁止 emoji**
5. **系列导航**：上一/下一篇硬边卡片；没有就保留 `disabled` 写法。
6. **首页列表**：在 `index.html` 的 `<div class="article-list">` 顶部插入新行（结构照抄现有行，`href` 和 `data-month` 属性都要改），并重排全部 `ar-num` 编号（01/02/03…）。
7. **归档筛选器**：文章区右侧是「文章归档」筛选器（`#af-body`），按月份过滤列表。新文章要：① 给它的 `.article-row` 加 `data-month="YYYY-MM"`；② 在 `#af-body` 里更新对应月份的按钮篇数（`.af-count`）；③ 没有对应月份就新增一个 `.af-btn` 按钮（按时间倒序插入），并更新「全部文章」的篇数。
8. **文章结尾**：**默认不加付费广告**（promo-block 锦囊妙计 / 微信引导 / 「请联系我」类话术一律不加）——联系方式在首页联系区，读者有需要会自己找。`footer-note` 预告可以保留。除非站长明确要求，否则不要加任何付费推广块。
9. **提交推送**：commit + push，Cloudflare 自动部署。

## 如何添加新服务

首页 `id="services"` 服务区（黄幕）结构：

- **服务网格**：`.svc-grid` 内复制 `.svc-item`（标签 + 标题 + 描述）。当前三项：AI 智能体开发（定制）、拆书智能体（已上线）、辅助写作智能体（已上线）。
- **付费咨询模块**：`.consult-card`（点击展开详情）。当前定价 **99元/小时**。改价格注意两处：卡片价格行 + 详情里的费用行。
- **微信引导**：`.svc-note`，微信号 `think99zj`。

## 首页联系区（联系方式）

首页 `id="contact"` 联系区（黑幕）结构：

- **文字链接**：`.contact-links` 两行——抖音（搜索绝顶赵）+ 微信（think99zj）。
- **二维码并排**：`.contact-qr` 两个 `.qr-col`（左微信右抖音粉丝问答群），图片在 `images/`（`WeChat.png`、`TikTok-group.png`，抖音原文件是 `TikTokgroup.jfif` 转 PNG 而来）。换码时替换图片文件即可，注意保持比例（CSS 已设 `height:auto`，图片勿强制拉伸）。

## 文章目录

| # | 文章 | 链接 | 日期 |
|---|---|---|---|
| 01 | 倪海厦经方中医技能（nihaixia）安装指南（AI 技能 · 面向 AI Agent） | `专栏/倪海厦经方中医技能（nihaixia）安装指南.html` | 2026-08-15 |
| 02 | 给 AI 安装口播技能（AI 技能 · 复制即用文档） | `专栏/口播技能-给AI安装口播文案生成器.html` | 2026-08-09 |
| 03 | Windows 上安装 Hermes 并启用 Mem0 动态记忆（AI 实操） | `专栏/Windows上安装Hermes启用Mem0动态记忆.html` | 2026-07-25 |
| 04 | 适应性平衡理论（核心理论 · V3.2） | `专栏/适应性平衡理论.html` | 2026-05-15 |
| 05 | 行动的方法（获得执行力 · 下） | `专栏/获得执行力（下）：行动的方法.html` | 2026-08-07 |
| 06 | 为什么我们无法行动？（获得执行力 · 中） | `专栏/获得执行力（中）：为什么我们无法行动？.html` | 2026-08-07 |
| 07 | 获得执行力（上） | `专栏/获得执行力（上）.html` | 2026-08-07 |

## 未来的计划

- **文章按时间归档**（按年份/月份归档文章列表）——**已实现**（2026-08-09）：文章区右侧筛选器按月份过滤，见上方维护流程。
- 付费咨询模块后续可能调整定价/名额机制。
- 理论文章若出 V4 版本，更新 `适应性平衡理论.html` 并同步首页摘要。

## 踩过的坑（务必记住）

1. **文章页正文不能依赖 JS 才显示**：v4 曾用 IntersectionObserver 触发正文淡入，长文章首屏触发阈值不足 → 正文全透明（线上事故）。修复后正文默认直接可见，只保留 hero 标题动画。**新文章页不要给正文加 reveal**。
2. **hero 视差层导致元素重叠**：滚动时不同速度的层会互相追叠（圆章压按钮、标题盖 CTA）。已停用 hero 全部视差。**不要恢复 hero 视差**。
3. **文件名的全角冒号/问号有 CDN 隐患**（见上）。
4. **Cloudflare 会对 HTML 注入 JSD 追踪脚本**（隐藏链接 + iframe），属正常现象，不是页面 bug。
5. **Cloudflare 会把 `.html` 请求 308 重定向成 clean URL**（去后缀），部署后线上地址和本地文件名不完全一样，属正常。
6. **`plan.md` 在 .gitignore 里**（计划文档不入库）；README 必须入库。

## 协作约定（重要）

- **AI 只改代码，站长负责验收**：AI 不跑截图、Lighthouse、浏览器审查、http server。改完交付，站长在浏览器/生产环境自行验证。
- **不要安装任何浏览器相关工具**（puppeteer/playwright/chromium 等），站长明确禁止。
- 每次改动：改完 → 自查（JS 语法 node 检查、标签闭合、emoji 零残留、SITE 占位完整）→ commit → push。
- 工具页（show/drive-test/test）的**交互 JS 零改动**原则：只允许换视觉壳（CSS/导航/页脚）。
- 文章正文内容一字不改（除非站长明确要求）。
- 验收通过前不 commit？——不，当前惯例是：改完自查通过即可提交推送，站长在生产环境验收，不满意再改再推。
